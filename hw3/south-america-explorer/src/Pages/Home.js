import React from 'react';

const Home = () => (
  <section className="hero" aria-labelledby="home-title">
    <h2 id="home-title">Welcome</h2>
    <p>
      Use the navbar to see a list of South American countries, their
      populations, and a custom chart of the largest countries by area.
    </p>
  </section>
);

export default Home;
